---
import { companyInfo } from "../../data/data";

const { phoneNumbers, email, address } = companyInfo();
const currentYear = new Date().getFullYear();
---

<footer class="bg-slate-900 text-white py-20 border-t-8 border-blue-600">
    <div class="bf-container">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-16">
            <div class="col-span-1 md:col-span-2">
                <div class="flex items-center gap-4 mb-6">
                    <div
                        class="w-12 h-12 bg-white flex items-center justify-center text-slate-900"
                    >
                        <span class="text-2xl font-black">N</span>
                    </div>
                    <span class="text-2xl font-black uppercase tracking-tighter"
                        >Navalpar</span
                    >
                </div>
                <p class="text-slate-400 max-w-sm font-bold text-lg">
                    Construyendo confianza desde la base.
                </p>
            </div>

            <div>
                <h4
                    class="text-sm font-black uppercase tracking-widest text-blue-500 mb-6"
                >
                    Menu
                </h4>
                <nav class="flex flex-col gap-4 font-bold">
                    <a
                        href="#inicio"
                        class="hover:text-blue-500 transition-colors">Inicio</a
                    >
                    <a
                        href="#servicios"
                        class="hover:text-blue-500 transition-colors"
                        >Servicios</a
                    >
                    <a
                        href="#contacto"
                        class="hover:text-blue-500 transition-colors"
                        >Contacto</a
                    >
                </nav>
            </div>

            <div>
                <h4
                    class="text-sm font-black uppercase tracking-widest text-blue-500 mb-6"
                >
                    Legal
                </h4>
                <nav class="flex flex-col gap-4 font-bold">
                    <a
                        href="/aviso-legal"
                        class="hover:text-blue-500 transition-colors"
                        >Aviso Legal</a
                    >
                    <a
                        href="/politica-de-privacidad"
                        class="hover:text-blue-500 transition-colors"
                        >Privacidad</a
                    >
                </nav>
            </div>
        </div>

        <div
            class="border-t-2 border-slate-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm font-bold text-slate-500 uppercase tracking-wide"
        >
            <p>&copy; {currentYear} Servicios Navalpar.</p>
        </div>
    </div>
</footer>
